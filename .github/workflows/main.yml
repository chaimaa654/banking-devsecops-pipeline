name: DVWA DevSecOps Pipeline

on:
  push:
    branches:
      - main

jobs:

  # =========================
  # 1️⃣ SAST – SonarCloud
  # ==========================
  sast_scan:
    name: SAST - SonarCloud Scan
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=chaimaa654_banking-devsecops-pipeline
            -Dsonar.organization=chaimaa654
            -Dsonar.sources=DVWA
            -Dsonar.qualitygate.wait=false

  # ==================================
  # 2️⃣ SCA – Trivy Filesystem Scan
  # ==================================
  sca_scan:
    name: SCA - Trivy Filesystem Scan
    runs-on: ubuntu-latest
    needs: sast_scan

    steps:
      - uses: actions/checkout@v3

      - name: Install Trivy
        run: |
          sudo apt-get update
          sudo apt-get install -y wget gnupg lsb-release
          wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | sudo apt-key add -
          echo deb https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main | sudo tee /etc/apt/sources.list.d/trivy.list
          sudo apt-get update
          sudo apt-get install -y trivy

      - name: Trivy SCA Scan
        run: trivy fs --severity HIGH,CRITICAL ./DVWA || true

      - name: Generate SCA Report
        run: trivy fs --format json --output trivy-sca-report.json ./DVWA

      - uses: actions/upload-artifact@v4
        with:
          name: trivy-sca-report
          path: trivy-sca-report.json

  # ==================================
  # 3️⃣ Image Build + Image Security
  # ==================================
  image_scan:
    name: Docker Image Build & Scan
    runs-on: ubuntu-latest
    needs: sca_scan

    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Validate and Clean Secrets
        id: secrets
        env:
          REPO_USER: ${{ secrets.REPO_USER }}
        run: |
          # Nettoyer et valider
          CLEAN_USER=$(echo "$REPO_USER" | tr -d '[:space:]' | tr -d '\n' | tr -d '\r')
          
          if [ -z "$CLEAN_USER" ]; then
            echo "❌ REPO_USER is empty after cleaning!"
            exit 1
          fi
          
          echo "✅ Clean username: ${CLEAN_USER}"
          echo "clean_user=${CLEAN_USER}" >> $GITHUB_OUTPUT

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ steps.secrets.outputs.clean_user }}
          password: ${{ secrets.REPO_PWD }}

      - name: Build Image
        run: docker build -t dvwa:latest ./DVWA

      - name: Tag and Push Image
        run: |
          docker tag dvwa:latest ${{ steps.secrets.outputs.clean_user }}/dvwa:latest
          docker push ${{ steps.secrets.outputs.clean_user }}/dvwa:latest

      - name: Trivy Image Scan
        run: trivy image --severity HIGH,CRITICAL dvwa:latest || true

  # =============================
  # 4️⃣ CD STAGING + DAST (ZAP)
  # =============================
  dast_scan:
    name: CD Staging + DAST (ZAP)
    runs-on: ubuntu-latest
    needs: image_scan

    steps:
      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq curl

      - name: Docker Login
        env:
          REPO_USER: ${{ secrets.REPO_USER }}
        run: |
          CLEAN_USER=$(echo "$REPO_USER" | tr -d '[:space:]')
          echo "${{ secrets.REPO_PWD }}" | docker login -u "$CLEAN_USER" --password-stdin

      - name: Pull Image
        env:
          REPO_USER: ${{ secrets.REPO_USER }}
        run: |
          CLEAN_USER=$(echo "$REPO_USER" | tr -d '[:space:]')
          docker pull ${CLEAN_USER}/dvwa:latest

      - name: Create Network
        run: docker network create dvwa-net || true

      - name: Run DVWA
        env:
          REPO_USER: ${{ secrets.REPO_USER }}
        run: |
          CLEAN_USER=$(echo "$REPO_USER" | tr -d '[:space:]')
          docker run -d \
            --name dvwa-staging \
            --network dvwa-net \
            -p 8080:80 \
            ${CLEAN_USER}/dvwa:latest

      - name: Wait for DVWA to Start
        run: |
          echo "🔍 Waiting for DVWA to be ready..."
          SUCCESS=false
          for i in {1..60}; do
            HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8080 2>/dev/null || echo "000")
            if [[ "$HTTP_CODE" == "200" ]] || [[ "$HTTP_CODE" == "302" ]]; then
              echo "✅ DVWA is ready! (HTTP $HTTP_CODE)"
              SUCCESS=true
              break
            fi
            echo "⏳ Attempt $i/60 - HTTP Code: $HTTP_CODE"
            sleep 5
          done
          
          if [ "$SUCCESS" = false ]; then
            echo "❌ DVWA failed to start!"
            docker logs dvwa-staging
            exit 1
          fi

      - name: Create ZAP Reports Directory
        run: |
          mkdir -p ${{ github.workspace }}/zap-reports
          chmod 777 ${{ github.workspace }}/zap-reports
          ls -la ${{ github.workspace }}/

      # 🔹 Passive Scan
      - name: ZAP Baseline Scan
        run: |
          echo "🔍 Running ZAP Baseline Scan (Passive)..."
          docker run --rm \
            --network dvwa-net \
            -v ${{ github.workspace }}/zap-reports:/zap/wrk:rw \
            ghcr.io/zaproxy/zap-stable:latest \
            zap-baseline.py \
            -t http://dvwa-staging:80 \
            -r zap-baseline.html \
            -J zap-baseline.json \
            -I || true
          
          echo "📁 Checking if baseline report was created..."
          ls -la ${{ github.workspace }}/zap-reports/ || echo "⚠️ Directory not accessible"

      # 🔹 Active Scan
      - name: ZAP Active Scan
        run: |
          echo "🎯 Running ZAP Full Active Scan..."
          docker run --rm \
            --network dvwa-net \
            -v ${{ github.workspace }}/zap-reports:/zap/wrk:rw \
            ghcr.io/zaproxy/zap-stable:latest \
            zap-full-scan.py \
            -t http://dvwa-staging:80 \
            -r zap-active.html \
            -J zap-active.json \
            -m 5 \
            -T 15 \
            -I || true
          
          echo "📁 Checking if active report was created..."
          ls -la ${{ github.workspace }}/zap-reports/ || echo "⚠️ Directory not accessible"

      # 📊 Verify Reports Exist
      - name: Verify ZAP Reports
        run: |
          echo "📊 Verifying ZAP reports..."
          echo ""
          if [ -f "${{ github.workspace }}/zap-reports/zap-baseline.html" ]; then
            echo "✅ zap-baseline.html found"
          else
            echo "❌ zap-baseline.html NOT found"
          fi
          
          if [ -f "${{ github.workspace }}/zap-reports/zap-baseline.json" ]; then
            echo "✅ zap-baseline.json found"
          else
            echo "❌ zap-baseline.json NOT found"
          fi
          
          if [ -f "${{ github.workspace }}/zap-reports/zap-active.html" ]; then
            echo "✅ zap-active.html found"
          else
            echo "❌ zap-active.html NOT found"
          fi
          
          if [ -f "${{ github.workspace }}/zap-reports/zap-active.json" ]; then
            echo "✅ zap-active.json found"
          else
            echo "❌ zap-active.json NOT found"
          fi
          
          echo ""
          echo "📁 All files in zap-reports:"
          ls -lah ${{ github.workspace }}/zap-reports/ || echo "Directory is empty or doesn't exist"

      # 📊 Analyze Results
      - name: Analyze ZAP Results
        run: |
          echo ""
          echo "📊 =========================="
          echo "📊 DAST SCAN RESULTS"
          echo "📊 =========================="
          
          analyze_report() {
            local report=$1
            local name=$2
            
            if [ -f "$report" ]; then
              echo ""
              echo "📄 $name:"
              echo "----------------------------"
              
              CRITICAL=$(jq '[.site[]?.alerts[]? | select(.riskcode=="4")] | length' "$report" 2>/dev/null || echo "0")
              HIGH=$(jq '[.site[]?.alerts[]? | select(.riskcode=="3")] | length' "$report" 2>/dev/null || echo "0")
              MEDIUM=$(jq '[.site[]?.alerts[]? | select(.riskcode=="2")] | length' "$report" 2>/dev/null || echo "0")
              LOW=$(jq '[.site[]?.alerts[]? | select(.riskcode=="1")] | length' "$report" 2>/dev/null || echo "0")
              
              echo "🔴 Critical: $CRITICAL"
              echo "🟠 High: $HIGH"
              echo "🟡 Medium: $MEDIUM"
              echo "🟢 Low: $LOW"
              
              if [ "$CRITICAL" -gt 0 ]; then
                echo ""
                echo "🚨 Critical Vulnerabilities:"
                jq -r '.site[]?.alerts[]? | select(.riskcode=="4") | "  - \(.name) (\(.count) instances)"' "$report" 2>/dev/null || true
              fi
              
              if [ "$HIGH" -gt 0 ]; then
                echo ""
                echo "⚠️  High Vulnerabilities:"
                jq -r '.site[]?.alerts[]? | select(.riskcode=="3") | "  - \(.name) (\(.count) instances)"' "$report" 2>/dev/null || true
              fi
            else
              echo "⚠️ Report $report not found"
            fi
          }
          
          analyze_report "${{ github.workspace }}/zap-reports/zap-baseline.json" "ZAP Baseline (Passive)"
          analyze_report "${{ github.workspace }}/zap-reports/zap-active.json" "ZAP Active Scan"

      # 🔒 Security Gate (Learning Mode)
      - name: DAST Security Gate
        run: |
          echo ""
          echo "🔓 =========================="
          echo "🔓 SECURITY GATE: LEARNING MODE"
          echo "🔓 =========================="
          
          TOTAL_CRITICAL=0
          TOTAL_HIGH=0
          
          if [ -f "${{ github.workspace }}/zap-reports/zap-baseline.json" ]; then
            BASELINE_CRIT=$(jq '[.site[]?.alerts[]? | select(.riskcode=="4")] | length' ${{ github.workspace }}/zap-reports/zap-baseline.json 2>/dev/null || echo "0")
            BASELINE_HIGH=$(jq '[.site[]?.alerts[]? | select(.riskcode=="3")] | length' ${{ github.workspace }}/zap-reports/zap-baseline.json 2>/dev/null || echo "0")
            TOTAL_CRITICAL=$((TOTAL_CRITICAL + BASELINE_CRIT))
            TOTAL_HIGH=$((TOTAL_HIGH + BASELINE_HIGH))
          fi
          
          if [ -f "${{ github.workspace }}/zap-reports/zap-active.json" ]; then
            ACTIVE_CRIT=$(jq '[.site[]?.alerts[]? | select(.riskcode=="4")] | length' ${{ github.workspace }}/zap-reports/zap-active.json 2>/dev/null || echo "0")
            ACTIVE_HIGH=$(jq '[.site[]?.alerts[]? | select(.riskcode=="3")] | length' ${{ github.workspace }}/zap-reports/zap-active.json 2>/dev/null || echo "0")
            TOTAL_CRITICAL=$((TOTAL_CRITICAL + ACTIVE_CRIT))
            TOTAL_HIGH=$((TOTAL_HIGH + ACTIVE_HIGH))
          fi
          
          echo "📊 Total Critical: $TOTAL_CRITICAL"
          echo "📊 Total High: $TOTAL_HIGH"
          echo ""
          echo "⚠️  DVWA is intentionally vulnerable"
          echo "🔓 Security Gate: DISABLED for learning"
          echo "✅ DAST scan completed - review reports for findings"
          echo ""
          echo "💡 For production: Set thresholds and enable blocking"

      - name: Upload ZAP Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-dast-report
          path: ${{ github.workspace }}/zap-reports/
          if-no-files-found: warn

      - name: Cleanup
        if: always()
        run: |
          echo "🧹 Cleaning up..."
          docker rm -f dvwa-staging || true
          docker network rm dvwa-net || true
          echo "✅ Cleanup completed"

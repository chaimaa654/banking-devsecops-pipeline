name: DVWA DevSecOps Pipeline

on:
  push:
    branches:
      - main

jobs:

  # =============================
  # 1️⃣ SAST – SonarCloud (NON BLOQUANT TEMPORAIRE)
  # =============================
  sast_scan:
    name: SAST - SonarCloud Scan (Non Bloquant)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'

      - name: SonarCloud Scan (Non Bloquant)
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=chaimaa654_banking-devsecops-pipeline
            -Dsonar.organization=chaimaa654
            -Dsonar.sources=DVWA
            -Dsonar.qualitygate.wait=false  

  # ==================================
  # 2️⃣ SCA – Trivy FS (NON BLOQUANT TEMPORAIRE)
  # ==================================
  sca_scan:
    name: SCA - Trivy Filesystem Scan (Non Bloquant)
    runs-on: ubuntu-latest
    needs: sast_scan

    steps:
      - uses: actions/checkout@v3

      - name: Trivy FS Scan (Non Bloquant)
        uses: aquasecurity/trivy-action@0.24.0
        with:
          scan-type: fs
          scan-ref: ./DVWA
          severity: HIGH,CRITICAL
          exit-code: 0  # Non bloquant temporaire
          format: json
          output: trivy-sca-report.json

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trivy-sca-report
          path: trivy-sca-report.json

  # ==================================
  # 3️⃣ IMAGE – Docker + Trivy (NON BLOQUANT TEMPORAIRE)
  # ==================================
  image_scan:
    name: Docker Image Build (Non Bloquant)
    runs-on: ubuntu-latest
    needs: sca_scan

    steps:
      - uses: actions/checkout@v3

      - name: Build DVWA Docker Image
        run: docker build -t dvwa:latest ./DVWA

      - name: Trivy Image Scan (Non Bloquant)
        uses: aquasecurity/trivy-action@0.24.0
        with:
          scan-type: image
          image-ref: dvwa:latest
          severity: HIGH,CRITICAL
          exit-code: 0
          format: json
          output: trivy-image-report.json

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trivy-image-report
          path: trivy-image-report.json

  # ==================================
  # 4️⃣ DAST – OWASP ZAP (PRINCIPAL)
  # ==================================
  dast_scan:
    name: DAST - OWASP ZAP
    runs-on: ubuntu-latest
    needs: image_scan

    steps:
      - uses: actions/checkout@v3

      # 🔹 Lancer DVWA dans un container
      - name: Run DVWA container
        run: docker run -d --name dvwa-staging -p 8080:80 dvwa:latest

      - name: Wait for DVWA
        run: sleep 15  # Temps pour que DVWA soit prêt

      # 🔹 Lancer ZAP Baseline Scan via l’action officielle
      - name: OWASP ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.7.0
        with:
          target: 'http://localhost:8080'
          format: 'html'
          output: 'zap-report.html'
          failAction: 'false'  # Non bloquant temporaire

      # 🔹 Upload rapport DAST
      - uses: actions/upload-artifact@v4
        with:
          name: zap-dast-report
          path: zap-report.html

      # 🔹 Stop DVWA container
      - name: Stop DVWA container
        run: docker rm -f dvwa-staging
